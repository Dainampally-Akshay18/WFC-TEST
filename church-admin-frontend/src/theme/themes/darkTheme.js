import { darkColors } from '../colors/dark';
import { darkGradients } from '../gradients/dark';
import { darkShadows } from '../shadows/dark';
import { glassStyles } from '../glass/glassmorphism';
import { fonts, fontSizes, fontWeights, lineHeights } from '../typography/fonts';
import { spacing } from '../spacing/spacing';
import { borderRadius } from '../radius/borderRadius';
import { transitions, animations } from '../animations/transitions';

export const darkTheme = {
  name: 'dark',
  colors: darkColors,
  gradients: darkGradients,
  shadows: darkShadows,
  glass: glassStyles.dark,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  spacing,
  borderRadius,
  transitions,
  animations,
};
