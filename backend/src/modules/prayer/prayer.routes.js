import { Router } from 'express';
import prayerController from './prayer.controller.js';

const prayerRouter = Router();

// TODO: Register routes
// prayerRouter.post('/', authMiddleware, prayerController.submitPrayer);
// prayerRouter.get('/:id', prayerController.getPrayer);
// prayerRouter.get('/', prayerController.getAllPrayers);
// prayerRouter.post('/:id/like', authMiddleware, prayerController.likePrayer);
// prayerRouter.delete('/:id', authMiddleware, prayerController.deletePrayer);

export default prayerRouter;
